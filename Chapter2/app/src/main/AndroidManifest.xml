<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.chapter_2"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".intent.ImplicitIntentActivity">
            <intent-filter>
                <action android:name="neo"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".intent.ui.login.LoginActivity"
            android:label="@string/title_activity_login">
        </activity>
        <activity android:name=".recyclerview.RecyclerViewAnimatorActivity"/>
        <activity android:name=".view.LayoutParamsActivity">
        </activity>
        <activity android:name=".view.AttributeActivity">
        </activity>
        <activity android:name=".view.ViewActivity"/>
        <activity android:name=".intent.ResultActivity">
        </activity>
        <activity android:name=".intent.IntentActivity">
        </activity>
        <activity android:name=".recyclerview.RecyclerViewActivity">
        </activity>
        <activity
            android:name=".lifecycle.LifecycleActivity"
            android:configChanges="keyboardHidden|orientation|screenSize">
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>